---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: loadbalancerrequests.butler.butlerlabs.dev
spec:
  group: butler.butlerlabs.dev
  names:
    kind: LoadBalancerRequest
    listKind: LoadBalancerRequestList
    plural: loadbalancerrequests
    shortNames:
    - lbr
    singular: loadbalancerrequest
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: Target cluster
      jsonPath: .spec.clusterName
      name: Cluster
      type: string
    - description: Current phase
      jsonPath: .status.phase
      name: Phase
      type: string
    - description: LB endpoint
      jsonPath: .status.endpoint
      name: Endpoint
      type: string
    - description: Registered backends
      jsonPath: .status.registeredTargets
      name: Targets
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          LoadBalancerRequest is the Schema for the loadbalancerrequests API.
          It represents a request to create a cloud load balancer for a management
          cluster's control plane endpoint. Provider controllers watch this resource
          and provision the appropriate cloud-native load balancer (GCP forwarding rule,
          AWS NLB, or Azure Standard LB).
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: |-
              LoadBalancerRequestSpec defines the desired state of a cloud load balancer
              for a management cluster control plane endpoint.
            properties:
              clusterName:
                description: |-
                  ClusterName is the name of the cluster this load balancer serves.
                  Used as a prefix for cloud resource naming. Must be DNS-safe since
                  cloud providers use it in resource names.
                maxLength: 63
                minLength: 1
                pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                type: string
              healthCheckPort:
                description: |-
                  HealthCheckPort is the port used for backend health checks.
                  Defaults to the same value as Port.
                format: int32
                maximum: 65535
                minimum: 1
                type: integer
              port:
                default: 6443
                description: Port is the target port on backend instances.
                format: int32
                maximum: 65535
                minimum: 1
                type: integer
              providerConfigRef:
                description: ProviderConfigRef references the ProviderConfig with
                  cloud credentials.
                properties:
                  name:
                    description: Name is the name of the ProviderConfig resource.
                    minLength: 1
                    type: string
                  namespace:
                    description: |-
                      Namespace is the namespace of the ProviderConfig resource.
                      If not specified, the namespace of the referencing resource is used.
                    type: string
                required:
                - name
                type: object
              targets:
                description: |-
                  Targets are the backend instances to register with the load balancer.
                  Updated incrementally as machines come online.
                items:
                  description: LoadBalancerTarget identifies a backend instance for
                    load balancer registration.
                  properties:
                    instanceID:
                      description: |-
                        InstanceID is the cloud-provider instance identifier.
                        Used for instance-based target groups (e.g., AWS NLB).
                      type: string
                    instanceName:
                      description: |-
                        InstanceName is the cloud-provider instance name.
                        Used for name-based registration (e.g., GCP target pools).
                      type: string
                    ip:
                      description: IP is the target IP address.
                      type: string
                  type: object
                type: array
            required:
            - clusterName
            - providerConfigRef
            type: object
            x-kubernetes-validations:
            - message: clusterName is immutable
              rule: self.clusterName == oldSelf.clusterName
            - message: providerConfigRef is immutable
              rule: self.providerConfigRef == oldSelf.providerConfigRef
          status:
            description: LoadBalancerRequestStatus defines the observed state of LoadBalancerRequest.
            properties:
              conditions:
                description: Conditions represent the latest available observations
                  of the request's state.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              endpoint:
                description: |-
                  Endpoint is the load balancer's IP address or DNS name.
                  Populated when Phase reaches Ready.
                type: string
              failureMessage:
                description: FailureMessage provides a human-readable failure message.
                type: string
              failureReason:
                description: FailureReason provides a machine-readable failure reason.
                type: string
              lastUpdated:
                description: LastUpdated is the timestamp of the last status update.
                format: date-time
                type: string
              observedGeneration:
                description: ObservedGeneration is the generation most recently observed
                  by the controller.
                format: int64
                type: integer
              phase:
                description: Phase represents the current lifecycle phase.
                enum:
                - Pending
                - Creating
                - Ready
                - Failed
                - Deleting
                type: string
              registeredTargets:
                description: RegisteredTargets is the number of targets currently
                  registered with the load balancer.
                format: int32
                type: integer
              resourceID:
                description: |-
                  ResourceID is the cloud-provider resource identifier for the load balancer.
                  Used during cleanup to locate the resource for deletion.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
