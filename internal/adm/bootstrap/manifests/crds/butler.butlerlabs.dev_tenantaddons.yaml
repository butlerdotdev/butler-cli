---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: tenantaddons.butler.butlerlabs.dev
spec:
  group: butler.butlerlabs.dev
  names:
    kind: TenantAddon
    listKind: TenantAddonList
    plural: tenantaddons
    shortNames:
    - ta
    singular: tenantaddon
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: Target cluster
      jsonPath: .spec.clusterRef.name
      name: Cluster
      type: string
    - description: Addon name
      jsonPath: .spec.addon
      name: Addon
      type: string
    - description: Desired version
      jsonPath: .spec.version
      name: Version
      type: string
    - description: Current phase
      jsonPath: .status.phase
      name: Phase
      type: string
    - description: Installed version
      jsonPath: .status.installedVersion
      name: Installed
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          TenantAddon is the Schema for the tenantaddons API.
          It represents an addon to be installed in a TenantCluster.
          Unlike addons defined in TenantCluster.spec.addons (which are monotonic),
          TenantAddons can be deleted to remove the addon from the cluster.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: TenantAddonSpec defines the desired state of TenantAddon.
            properties:
              addon:
                description: |-
                  Addon specifies a known Butler addon name.
                  Use this for built-in addons like cilium, metallb, etc.
                  Mutually exclusive with Helm.
                type: string
              clusterRef:
                description: ClusterRef references the TenantCluster to install this
                  addon into.
                properties:
                  name:
                    description: Name is the name of the resource.
                    minLength: 1
                    type: string
                required:
                - name
                type: object
              dependsOn:
                description: DependsOn specifies other TenantAddons that must be ready
                  first.
                items:
                  description: LocalObjectReference references a resource in the same
                    namespace.
                  properties:
                    name:
                      description: Name is the name of the resource.
                      minLength: 1
                      type: string
                  required:
                  - name
                  type: object
                type: array
              helm:
                description: |-
                  Helm specifies a custom Helm chart to install.
                  Use this for arbitrary Helm charts not built into Butler.
                  Mutually exclusive with Addon.
                properties:
                  chart:
                    description: Chart is the chart name within the repository.
                    type: string
                  createNamespace:
                    default: true
                    description: CreateNamespace creates the namespace if it doesn't
                      exist.
                    type: boolean
                  namespace:
                    description: |-
                      Namespace is the target namespace for the Helm release.
                      If not specified, a namespace is chosen based on the chart.
                    type: string
                  releaseName:
                    description: |-
                      ReleaseName is the Helm release name.
                      If not specified, defaults to the TenantAddon name.
                    type: string
                  repository:
                    description: Repository is the Helm repository URL.
                    type: string
                required:
                - chart
                - repository
                type: object
              values:
                description: Values are Helm values for customization.
                type: object
                x-kubernetes-preserve-unknown-fields: true
              version:
                description: Version is the addon version to install.
                type: string
            required:
            - clusterRef
            - version
            type: object
          status:
            description: TenantAddonStatus defines the observed state of TenantAddon.
            properties:
              conditions:
                description: Conditions represent the latest available observations.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              helmRelease:
                description: HelmRelease contains Helm release information.
                properties:
                  name:
                    description: Name is the name of the Helm release
                    type: string
                  namespace:
                    description: Namespace is the namespace where the release is installed
                    type: string
                  revision:
                    description: Revision is the Helm release revision number
                    type: integer
                  status:
                    description: Status is the Helm release status (deployed, failed,
                      etc.)
                    type: string
                  version:
                    description: Version is the chart version
                    type: string
                type: object
              installedVersion:
                description: InstalledVersion is the currently installed version.
                type: string
              lastTransitionTime:
                description: LastTransitionTime is when the phase last changed.
                format: date-time
                type: string
              message:
                description: Message provides human-readable status information.
                type: string
              observedGeneration:
                description: ObservedGeneration is the last observed generation.
                format: int64
                type: integer
              phase:
                description: Phase represents the current phase of the addon.
                enum:
                - Pending
                - Installing
                - Installed
                - Upgrading
                - Degraded
                - Failed
                - Deleting
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
