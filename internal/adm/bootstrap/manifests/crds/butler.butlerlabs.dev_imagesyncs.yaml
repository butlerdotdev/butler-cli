---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: imagesyncs.butler.butlerlabs.dev
spec:
  group: butler.butlerlabs.dev
  names:
    kind: ImageSync
    listKind: ImageSyncList
    plural: imagesyncs
    shortNames:
    - is
    singular: imagesync
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: Sync phase
      jsonPath: .status.phase
      name: Phase
      type: string
    - description: Schematic ID
      jsonPath: .spec.factoryRef.schematicID
      name: Schematic
      priority: 1
      type: string
    - description: OS version
      jsonPath: .spec.factoryRef.version
      name: Version
      type: string
    - description: Target provider
      jsonPath: .spec.providerConfigRef.name
      name: Provider
      type: string
    - description: Provider image reference
      jsonPath: .status.providerImageRef
      name: Image Ref
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          ImageSync is the Schema for the imagesyncs API.
          It represents a request to sync an image from the Butler Image Factory
          to an infrastructure provider.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ImageSyncSpec defines the desired state of ImageSync.
            properties:
              displayName:
                description: |-
                  DisplayName is the human-readable name for the image on the provider.
                  If not set, auto-generated from OS type + version + schematic ID prefix.
                type: string
              factoryRef:
                description: FactoryRef identifies the image in Butler Image Factory.
                properties:
                  arch:
                    default: amd64
                    description: Arch is the CPU architecture.
                    enum:
                    - amd64
                    - arm64
                    type: string
                  platform:
                    default: nocloud
                    description: |-
                      Platform is the target platform for the image artifact.
                      Maps to the Talos Image Factory platform identifier (e.g., "nocloud", "metal", "vmware").
                      Defaults to "nocloud" which works for KubeVirt/cloud-init environments (Harvester, Nutanix).
                    type: string
                  schematicID:
                    description: SchematicID is the content-addressable schematic
                      identifier (SHA-256 hex).
                    minLength: 8
                    type: string
                  version:
                    description: Version is the OS version (e.g., "v1.12.4", "9.5").
                    type: string
                required:
                - schematicID
                - version
                type: object
              format:
                default: qcow2
                description: Format is the image format to sync (e.g., "qcow2", "raw",
                  "iso").
                type: string
              providerConfigRef:
                description: ProviderConfigRef references the target ProviderConfig.
                properties:
                  name:
                    description: Name is the name of the ProviderConfig resource.
                    minLength: 1
                    type: string
                  namespace:
                    description: |-
                      Namespace is the namespace of the ProviderConfig resource.
                      If not specified, the namespace of the referencing resource is used.
                    type: string
                required:
                - name
                type: object
              transferMode:
                default: direct
                description: |-
                  TransferMode controls how the image artifact is transferred to the provider.
                  "direct" means the provider downloads directly from the factory URL.
                  "proxy" means the provider controller downloads first, then uploads to the provider.
                enum:
                - direct
                - proxy
                type: string
            required:
            - factoryRef
            - providerConfigRef
            type: object
          status:
            description: ImageSyncStatus defines the observed state of ImageSync.
            properties:
              artifactSHA256:
                description: ArtifactSHA256 is the verified checksum.
                type: string
              artifactURL:
                description: ArtifactURL is the factory download URL used.
                type: string
              conditions:
                description: Conditions represent the latest available observations.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              failureMessage:
                description: FailureMessage provides a human-readable failure message.
                type: string
              failureReason:
                description: FailureReason provides a machine-readable failure reason.
                type: string
              lastUpdated:
                description: LastUpdated is the timestamp of the last status update.
                format: date-time
                type: string
              observedGeneration:
                description: ObservedGeneration is the generation most recently observed
                  by the controller.
                format: int64
                type: integer
              phase:
                description: Phase represents the current lifecycle phase.
                enum:
                - Pending
                - Building
                - Downloading
                - Uploading
                - Ready
                - Failed
                type: string
              providerImageRef:
                description: |-
                  ProviderImageRef is the provider-specific image reference once synced.
                  Harvester: "namespace/name", Nutanix: UUID, Proxmox: template ID.
                type: string
              providerTaskID:
                description: |-
                  ProviderTaskID tracks a provider-side async task (e.g., Nutanix Prism Central task UUID).
                  Set during Downloading/Uploading phases, cleared on completion or failure.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
